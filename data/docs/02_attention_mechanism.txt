The Attention Mechanism calculates a weighted sum of values, where the weight assigned to each value is computed by a query with the corresponding key. Scaled Dot-Product Attention computes query-key dot products scaled by the square root of the key dimension, followed by a softmax function to obtain weights. Multi-Head Attention projects queries, keys, and values h times with different learned linear projections, allowing the network to capture complex contextual dependencies.
